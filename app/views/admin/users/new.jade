.page-header
  h1 New user

- form_for(user, {enctype:"multipart/form-data",action: path_to.admin_users(), method: 'POST', id: "user_form",class:'form-horizontal'}, function (form) {
  != partial('users/form.jade', {locals: {form: form, user: user}})
  .form-actions
    != form.submit('Create user', {class: 'btn btn-success'})
- });
