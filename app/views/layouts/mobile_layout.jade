!!! 5
html
  head
    title= title
    != javascript_include_tag('http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js', 'rails', 'jquery.cookie')
    != csrf_meta_tag()
    meta(name='viewport', content="width=device-width, initial-scale=1, maximum-scale=1,user-scalable=no")
    meta(name='apple-mobile-web-app-capable',content='yes')
    style(type='text/css')
      body{margin:0px;}
  body
    script(type='text/javascript',src='/socket.io/socket.io.js')
    - if (game.type == Game.type.invader) {
      != javascript_include_tag('arctic','invader','game-transceiver')
    - } else if(game.type == Game.type.halloween) {
      != javascript_include_tag('arctic','halloween','halloween-transceiver')
    - } else if(game.type == Game.type.monstar ){
      != javascript_include_tag('arctic','monstar','halloween-transceiver')
    - } else if(game.type == Game.type.pop){
      != javascript_include_tag('arctic','pop','halloween-transceiver')
    - }
    canvas#canvas
