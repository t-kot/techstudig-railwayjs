.page-header
  h1 Edit user

- form_for(user, {action: path_to.user(user), method: 'PUT', id: "user_form",enctype: "multipart/form-data"}, function (form) {
  != partial('users/form.jade', {locals: {form: form, user: user}})
  .actions
    != form.submit('Update user', {class: 'btn primary'})
- });
